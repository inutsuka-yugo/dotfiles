[core]
    # Set vim as the default editor
    editor = vim
    excludesfile = ~/.gitignore_global
    autocrlf = false    # Use LF for linux/macOS
    filemode = false    # Ignore chmod changes

[color]
    ui = auto

[init]
    defaultBranch = main

[push]
    default = simple

[pull]
    ff = only

# Include local gitconfig if it exists
[include]
    path = ~/.gitconfig.local


[alias]
    s = status
    co = checkout
    br = branch
    ci = commit
    st = status
    last = log -1 HEAD
    graph = log --graph --date=short --decorate=short --pretty=format:'%Cgreen%h %Creset%cd %Cblue%cn %Cred%d %Creset%s'

[credential]
	helper = cache
